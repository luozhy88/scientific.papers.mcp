# 📧 QQ 邮箱 SMTP 配置指南

## ⚠️ 当前问题

邮件发送出现 "Login denied" 错误，说明 **QQ 邮箱授权码** 配置有误。

## 🔑 获取 QQ 邮箱授权码（完整步骤）

### 步骤 1：登录 QQ 邮箱
访问：https://mail.qq.com/

### 步骤 2：进入设置
点击页面顶部的 **设置** 按钮（齿轮图标）

### 步骤 3：打开账户设置
在设置页面中，点击 **账户** 标签

### 步骤 4：找到服务设置
向下滚动，找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务** 部分

### 步骤 5：开启 SMTP 服务
- 找到 **IMAP/SMTP服务** 一行
- 如果显示"未开启"，点击 **开启** 按钮
- 如果已开启，跳到步骤 6

### 步骤 6：生成授权码
- 点击 **生成授权码** 按钮
- 验证手机号（发送短信验证码）
- 输入验证码后，会显示一个 **16 位授权码**

**示例授权码格式**：
```
abcd efgh ijkl mnop
```

**重要**：
- 这个授权码**不是你的 QQ 密码**
- 授权码只显示一次，请妥善保存
- 使用时**去掉空格**：`abcdefghijklmnop`

### 步骤 7：配置授权码

```bash
# 在终端中设置环境变量（替换为你的实际授权码）
export QQ_AUTH_CODE="你的16位授权码（无空格）"

# 设置收件人邮箱（可以是同一个邮箱）
export RECIPIENT_EMAIL="479321347@qq.com"

# 验证设置
echo $QQ_AUTH_CODE
echo $RECIPIENT_EMAIL
```

### 步骤 8：测试邮件功能

```bash
# 运行测试脚本
Rscript test_email.R
```

**成功示例输出**：
```
✅ 邮件发送成功！
   请检查邮箱：479321347@qq.com
   （包括垃圾邮件文件夹）
```

## 🔧 永久配置（推荐）

为了避免每次都要重新设置环境变量，可以将配置写入 shell 配置文件：

### bash 用户
```bash
echo 'export QQ_AUTH_CODE="你的授权码"' >> ~/.bashrc
echo 'export RECIPIENT_EMAIL="479321347@qq.com"' >> ~/.bashrc
source ~/.bashrc
```

### zsh 用户
```bash
echo 'export QQ_AUTH_CODE="你的授权码"' >> ~/.zshrc
echo 'export RECIPIENT_EMAIL="479321347@qq.com"' >> ~/.zshrc
source ~/.zshrc
```

## 🚀 运行文献搜索

配置完成后，运行搜索命令会自动发送邮件：

```bash
Rscript download_papersv13.R "machine learning" 5 10
```

**期望输出**：
```
[09:55:20] ℹ 准备发送邮件报告...
[09:55:20] ℹ 正在生成邮件报告...
[09:55:20] ℹ 发送邮件到: 479321347@qq.com
[09:55:23] ✔ 邮件发送成功！

✅ 完成!
```

## 📁 备用方案：保存到文件

如果授权码配置失败，脚本会**自动保存邮件内容到文件**：

```
[09:55:20] ⚠ 未配置 QQ 邮箱授权码，保存邮件内容到文件
[09:55:20] ✔ 邮件内容已保存到: output/email_report_20260218_095520.txt
[09:55:20] ⚠ 请手动将此文件内容复制到邮件中发送
```

然后您可以：
1. 打开保存的文本文件
2. 复制内容
3. 手动发送到邮箱

## ❓ 常见问题

### Q1: 为什么需要授权码而不是 QQ 密码？

**A**: 出于安全考虑，QQ 邮箱不允许第三方应用直接使用 QQ 密码登录。授权码相当于一个专用密码，即使泄露也不会影响你的 QQ 账号安全。

### Q2: 授权码忘记了怎么办？

**A**: 重新生成即可。旧的授权码会失效，不影响已有配置。

### Q3: 提示 "服务未开启"？

**A**: 需要在 QQ 邮箱设置中先开启 IMAP/SMTP 服务，然后才能生成授权码。

### Q4: 邮件发到垃圾箱了？

**A**: 第一次发送可能会被识别为垃圾邮件。在垃圾箱中找到邮件，标记为"非垃圾邮件"即可。

### Q5: 想发送到其他邮箱？

**A**: 修改环境变量：
```bash
export RECIPIENT_EMAIL="目标邮箱@example.com"
```

### Q6: 完全禁用邮件功能？

**A**: 编辑脚本，修改配置：
```r
EMAIL_ENABLED = FALSE
```

## 🔒 安全建议

1. **不要将授权码直接写在脚本中**（已使用环境变量）
2. **不要在公共代码仓库中提交授权码**
3. **定期更换授权码**（建议每 3-6 个月）
4. **如果授权码泄露，立即重新生成**

## 📞 技术支持

- QQ 邮箱帮助中心：https://service.mail.qq.com/
- SMTP 配置说明：https://service.mail.qq.com/detail/0/310

---

**配置完成后，重新运行测试：**

```bash
# 1. 设置授权码
export QQ_AUTH_CODE="你的新授权码"

# 2. 测试邮件
Rscript test_email.R

# 3. 运行文献搜索
Rscript download_papersv13.R "测试" 2 7
```

祝您使用顺利！🎉
