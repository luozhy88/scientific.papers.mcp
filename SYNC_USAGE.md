# 🚀 GitHub 同步脚本使用指南

## 📋 功能特性

优化后的 `sync-to-github.sh` 脚本包含以下增强功能：

### ✨ 核心功能
1. **自动检测当前分支** - 无需手动指定
2. **智能提交处理** - 无变更时自动跳过
3. **Rebase 模式拉取** - 保持提交历史整洁
4. **自动推送到远程** - 支持新分支创建

### 🔒 安全功能
- **大文件检测** - 超过 50MB 会提示警告
- **Stash 保护** - 拉取前自动保存未提交的修改
- **冲突提示** - 详细的冲突解决指导

### 🌐 网络优化
- **网络连接检测** - 推送前检查连接
- **自动重试机制** - 网络失败自动重试 3 次
- **重试间隔** - 每次重试间隔 5 秒

### 🌿 分支管理
- **远程分支检测** - 自动检测并创建新分支
- **子模块支持** - 自动更新 Git 子模块

## 🛠️ 使用方法

### 1. 添加执行权限
```bash
chmod +x sync-to-github.sh
```

### 2. 基本用法
```bash
# 使用默认提交信息（时间戳）
./sync-to-github.sh

# 自定义提交信息
./sync-to-github.sh "feat: 添加新功能"
```

### 3. 常见场景

#### 场景 1: 日常代码同步
```bash
./sync-to-github.sh "update: 更新文档"
```

#### 场景 2: 无新提交时同步
脚本会自动检测，如果没有新的修改，会跳过提交步骤，直接同步远程更新。

#### 场景 3: 首次推送新分支
```bash
git checkout -b feature/new-feature
# 进行修改...
./sync-to-github.sh "feat: 新功能开发"
```
脚本会自动检测并创建远程分支。

## ⚙️ 配置选项

在脚本开头可以修改以下配置：

```bash
# 遇到错误是否立即退出
EXIT_ON_ERROR=true

# 推送失败后的重试次数
MAX_RETRY=3

# 重试间隔（秒）
RETRY_DELAY=5

# 大文件警告阈值（MB）
MAX_FILE_SIZE=50
```

## 🔧 故障排除

### 问题 1: 推送失败
**原因**: 网络问题或权限不足
**解决**: 脚本会自动重试 3 次，如果仍然失败，请检查：
- 网络连接
- SSH 密钥配置
- 仓库访问权限

### 问题 2: 合并冲突
**原因**: 远程分支有新的提交
**解决**: 脚本会显示详细提示：
```bash
1. 查看状态: git status
2. 解决冲突后: git rebase --continue
3. 或者放弃 rebase: git rebase --abort
```

### 问题 3: 大文件警告
**原因**: 尝试提交超过 50MB 的文件
**解决**:
- 使用 Git LFS 管理大文件
- 将大文件添加到 `.gitignore`
- 或选择继续提交（不推荐）

### 问题 4: 网络连接失败
**原因**: 无法访问 GitHub
**解决**:
- 检查网络连接
- 如果在国内，可能需要配置代理
- 检查 DNS 设置

## 📝 工作流程

脚本执行顺序：

```
1. 初始化检查
   ├─ 检查 Git 仓库
   ├─ 获取当前分支
   └─ 检查远程分支

2. 提交变更
   ├─ 添加所有变更
   ├─ 检查大文件
   └─ 创建提交

3. 拉取更新 (如果远程分支存在)
   ├─ Stash 未提交的修改
   ├─ Rebase 模式拉取
   ├─ 恢复 Stash
   └─ 更新子模块

4. 推送到远程
   ├─ 检查网络连接
   ├─ 推送（支持重试）
   └─ 完成同步
```

## 💡 最佳实践

1. **定期同步**: 建议每天至少同步一次，避免积累太多冲突
2. **有意义的提交信息**: 使用清晰的提交信息，便于追踪
3. **小步提交**: 避免一次提交过多文件
4. **定期清理**: 删除不需要的文件，保持仓库整洁
5. **备份重要数据**: 在进行大规模修改前，确保有备份

## 🔗 相关命令

```bash
# 查看当前状态
git status

# 查看提交历史
git log --oneline -10

# 查看远程仓库
git remote -v

# 查看分支
git branch -a

# 查看 stash 列表
git stash list

# 强制拉取（谨慎使用）
git fetch origin
git reset --hard origin/main
```

## 📞 需要帮助？

如果遇到问题：
1. 检查脚本输出的错误信息
2. 使用 `git status` 查看当前状态
3. 查看本文档的故障排除部分
4. 检查 Git 配置：`git config --list`

---

**注意**: 此脚本会自动执行 `git add -A`，请确保 `.gitignore` 文件配置正确，避免提交敏感文件。
