# Scientific Papers MCP - 本地使用说明

## 📖 简介

Scientific Papers MCP 是一个用于搜索和获取学术论文的工具，支持多个学术数据源：
- **CORE** - 开放获取论文聚合
- **arXiv** - 计算机科学、物理、数学预印本
- **OpenAlex** - 学术文献图谱
- **Europe PMC** - 欧洲生物医学文献
- **PMC** - PubMed Central

## 🚀 快速开始

### 1. 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0

### 2. 启动工具

在项目目录下运行：

```bash
# 方式1：使用启动脚本
./run.sh <命令> [选项]

# 方式2：直接使用 Node.js
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
node paper-cli.mjs <命令> [选项]
```

## 📚 命令说明

### 1. 搜索论文 (search)

搜索指定数据源的论文。

**基本语法：**
```bash
./run.sh search --source=<数据源> --query="<关键词>" --count=<数量>
```

**参数说明：**
- `--source` - 数据源 (core, arxiv, openalex, europepmc)
- `--query` - 搜索关键词
- `--count` - 返回论文数量 (默认5，最大200)
- `--field` - 搜索字段 (all, title, abstract, author, fulltext)
- `--sort` - 排序方式 (relevance, date, citations)

**示例：**
```bash
# CORE 搜索 COVID-19
./run.sh search --source=core --query="COVID-19" --count=5

# arXiv 搜索机器学习
./run.sh search --source=arxiv --query="machine learning" --count=10

# 搜索标题中包含特定词
./run.sh search --source=core --query="neural networks" --field=title --count=5

# 按引用量排序
./run.sh search --source=openalex --query="AI" --sort=citations --count=10
```

### 2. 获取最新论文 (latest)

获取指定分类的最新论文（仅 arXiv 支持）。

**基本语法：**
```bash
./run.sh latest --source=arxiv --category=<分类> --count=<数量>
```

**常用分类：**
- `cs.AI` - 人工智能
- `cs.CV` - 计算机视觉
- `cs.LG` - 机器学习
- `cs.CL` - 计算语言学
- `cs.RO` - 机器人学
- `q-bio.BM` - 生物分子

**示例：**
```bash
# 获取 AI 领域最新论文
./run.sh latest --source=arxiv --category=cs.AI --count=5

# 获取计算机视觉最新论文
./run.sh latest --source=arxiv --category=cs.CV --count=10

# 获取生物信息学最新论文
./run.sh latest --source=arxiv --category=q-bio.BM --count=5
```

### 3. 获取论文内容 (content)

获取特定论文的详细内容（包括全文）。

**基本语法：**
```bash
./run.sh content --source=<数据源> --id=<论文ID>
```

**示例：**
```bash
# 获取 arXiv 论文内容
./run.sh content --source=arxiv --id=2602.12281

# 获取 CORE 论文内容
./run.sh content --source=core --id=85660944
```

### 4. 列出分类 (categories)

列出数据源支持的分类。

**基本语法：**
```bash
./run.sh categories --source=<数据源>
```

**示例：**
```bash
./run.sh categories --source=arxiv
./run.sh categories --source=core
```

## 🔧 高级用法

### 获取论文全文并保存到文件

```bash
# 搜索并保存到文件
./run.sh search --source=core --query="COVID-19" --count=5 > ~/Desktop/covid_papers.txt

# 获取最新论文并保存
./run.sh latest --source=arxiv --category=cs.AI --count=10 > ~/Desktop/ai_papers.txt
```

### 批量获取论文全文

使用提供的测试脚本批量获取：

```bash
# 进入项目目录
cd /Users/apple/Desktop/kimi/test3/Scientific-Papers-MCP

# 加载 Node.js 环境
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# 运行批量获取脚本
node test_fulltext.mjs
```

### 自定义搜索脚本

创建自己的搜索脚本 `my_search.mjs`：

```javascript
import { searchPapers } from './dist/tools/search-papers.js';
import { fetchContent } from './dist/tools/fetch-content.js';
import { RateLimiter } from './dist/core/rate-limiter.js';
import fs from 'fs';

const rateLimiter = new RateLimiter();

async function searchAndSave() {
  // 搜索论文
  const result = await searchPapers({
    source: 'core',
    query: '你的关键词',
    field: 'all',
    count: 5
  }, rateLimiter);
  
  const papers = result.content || [];
  
  // 获取每篇论文的全文
  for (const paper of papers) {
    try {
      const contentResult = await fetchContent({
        source: 'core',
        id: paper.id
      }, rateLimiter);
      
      const fullPaper = contentResult.content;
      if (fullPaper && fullPaper.text) {
        paper.text = fullPaper.text;
      }
    } catch (e) {
      console.error(`获取失败: ${paper.id}`);
    }
    
    // 延迟避免限流
    await new Promise(r => setTimeout(r, 2000));
  }
  
  // 保存到文件
  fs.writeFileSync('results.txt', JSON.stringify(papers, null, 2));
  console.log('保存完成！');
}

searchAndSave();
```

运行：
```bash
node my_search.mjs
```

## 📊 数据源对比

| 数据源 | 搜索 | 最新论文 | 获取全文 | 适用场景 |
|--------|------|----------|----------|----------|
| CORE | ✅ | ❌ | ✅ | 综合搜索，全文质量高 |
| arXiv | ⚠️ | ✅ | ✅ | 获取最新研究 |
| OpenAlex | ✅ | ❌ | ⚠️ | 学术文献图谱 |
| Europe PMC | ✅ | ❌ | ⚠️ | 医学文献 |
| PMC | ❌ | ✅ | ✅ | 生物医学 |

## ⚠️ 注意事项

### 1. API 限流

- **arXiv**: 较严格，建议每次请求间隔 2-3 秒
- **CORE**: 公开 API 有限制，建议配置 API Key
- **OpenAlex**: 相对宽松

**避免限流的方法：**
```javascript
// 在请求之间添加延迟
await new Promise(r => setTimeout(r, 2000));
```

### 2. 全文获取

- 不是所有论文都能获取全文
- 开放获取 (Open Access) 论文成功率较高
- 部分论文可能只有摘要

### 3. 网络问题

- 某些数据源可能需要代理访问
- PDF 下载可能因网络限制失败
- 可以复制 PDF 链接手动下载

### 4. 输出目录

测试结果默认保存到：`/Users/apple/Desktop/kimi/test3/output/`

## 📝 常见问题

### Q: 如何获取更多的论文？

A: 增加 `--count` 参数，最大支持 200 篇：
```bash
./run.sh search --source=core --query="AI" --count=200
```

### Q: 如何只搜索标题？

A: 使用 `--field=title` 参数：
```bash
./run.sh search --source=core --query="neural networks" --field=title --count=10
```

### Q: 如何按日期排序？

A: 使用 `--sort=date` 参数：
```bash
./run.sh search --source=core --query="COVID-19" --sort=date --count=10
```

### Q: 为什么有些论文获取不到全文？

A: 可能原因：
1. 论文不是开放获取
2. 需要付费订阅
3. 数据源没有收录全文
4. 网络访问受限

### Q: 如何下载 PDF 文件？

A: 使用获取到的 PDF 链接手动下载：
```bash
# 从输出结果中获取 PDF 链接
curl -O https://arxiv.org/pdf/论文ID.pdf
```

## 🔗 相关链接

- 项目地址：https://github.com/benedict2310/Scientific-Papers-MCP
- arXiv：https://arxiv.org/
- CORE：https://core.ac.uk/
- OpenAlex：https://openalex.org/

## 📧 技术支持

如有问题，请查看：
1. 项目 GitHub 页面的 Issues
2. 各数据源的 API 文档
3. 本使用说明的示例代码

---

**版本**：v1.0  
**更新日期**：2026-02-14  
**作者**：Scientific Papers MCP  
