# Scientific Papers MCP - 快速参考卡

## 🎯 常用命令速查

### 搜索论文
```bash
# CORE 搜索
./run.sh search --source=core --query="COVID-19" --count=5

# arXiv 搜索
./run.sh search --source=arxiv --query="machine learning" --count=5

# OpenAlex 搜索
./run.sh search --source=openalex --query="AI" --count=5
```

### 获取最新论文
```bash
# AI 领域最新论文
./run.sh latest --source=arxiv --category=cs.AI --count=5

# 计算机视觉
./run.sh latest --source=arxiv --category=cs.CV --count=5

# 自然语言处理
./run.sh latest --source=arxiv --category=cs.CL --count=5
```

### 获取论文全文
```bash
# 获取特定论文
./run.sh content --source=arxiv --id=2602.12281
./run.sh content --source=core --id=85660944
```

## 📋 数据源速查

| 数据源 | 命令 | 特点 |
|--------|------|------|
| CORE | `--source=core` | 全文获取好 |
| arXiv | `--source=arxiv` | 最新论文 |
| OpenAlex | `--source=openalex` | 文献图谱 |
| Europe PMC | `--source=europepmc` | 医学文献 |

## 🔍 高级搜索

```bash
# 搜索标题
./run.sh search --source=core --query="neural networks" --field=title --count=10

# 搜索摘要
./run.sh search --source=core --query="deep learning" --field=abstract --count=10

# 搜索作者
./run.sh search --source=core --query="Geoffrey Hinton" --field=author --count=10

# 按日期排序
./run.sh search --source=core --query="AI" --sort=date --count=10

# 按引用量排序
./run.sh search --source=openalex --query="AI" --sort=citations --count=10
```

## 🗂️ arXiv 分类速查

| 分类代码 | 说明 |
|----------|------|
| cs.AI | 人工智能 |
| cs.CV | 计算机视觉 |
| cs.LG | 机器学习 |
| cs.CL | 计算语言学 |
| cs.RO | 机器人学 |
| cs.DB | 数据库 |
| cs.DC | 分布式计算 |
| cs.CR | 密码学 |
| cs.HC | 人机交互 |
| cs.IR | 信息检索 |
| q-bio.BM | 生物分子 |
| q-bio.GN | 基因组学 |
| stat.ML | 统计机器学习 |

## 💾 保存结果

```bash
# 保存到文件
./run.sh search --source=core --query="COVID-19" --count=10 > ~/Desktop/results.txt

# 保存并查看
./run.sh latest --source=arxiv --category=cs.AI --count=5 > ~/Desktop/ai_latest.txt
cat ~/Desktop/ai_latest.txt
```

## ⏱️ 避免限流

```bash
# 在脚本中添加延迟
for i in {1..5}; do
  ./run.sh search --source=core --query="AI" --count=1 >> results.txt
  sleep 3  # 等待3秒
done
```

## 🔗 PDF 下载

```bash
# 手动下载 PDF（使用论文ID）
curl -O https://arxiv.org/pdf/2602.12281.pdf

# 或者使用 wget
wget https://arxiv.org/pdf/2602.12281.pdf
```

## 🆘 故障排除

| 问题 | 解决方案 |
|------|----------|
| API 限流 | 添加延迟（sleep 3） |
| 获取不到全文 | 尝试其他数据源 |
| 命令找不到 | 检查是否在项目目录下 |
| Node.js 错误 | 运行 `nvm use --lts` |

## 📁 项目位置

```
/Users/apple/Desktop/kimi/test3/Scientific-Papers-MCP/
├── 使用说明.md       # 详细使用说明
├── 快速参考.md       # 本文件
├── paper-cli.mjs     # CLI 工具
├── run.sh            # 启动脚本
└── dist/             # 编译后的代码
```

## 💡 使用技巧

1. **组合搜索**：先用 CORE 搜索，再用 arXiv 获取最新
2. **批量获取**：使用提供的测试脚本批量获取全文
3. **保存结果**：重定向输出到文件方便后续查看
4. **分类浏览**：使用 `categories` 命令查看可用分类

---

**提示**：更详细的说明请查看 `使用说明.md`
