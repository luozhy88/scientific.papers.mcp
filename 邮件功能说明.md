# 📧 文献搜索邮件功能说明

## 功能概述

`download_papersv13.R` 现已集成邮件自动发送功能。当文献搜索和PDF下载完成后，系统会自动将搜索结果以邮件形式发送到指定邮箱。

## 邮件内容

邮件报告包含以下信息：

1. **搜索概要**
   - 搜索关键词
   - 搜索日期
   - 总论文数
   - PDF下载成功数
   - 总下载大小

2. **文献详情**（最多20篇）
   - 📄 文章标题
   - 📅 发表日期
   - 📝 摘要（前300字）
   - 🔗 文献来源（Semantic Scholar、Europe PMC、arXiv等）
   - 📥 PDF下载状态
   - 🌐 文献链接

## 配置说明

### 当前使用的邮件服务

脚本已配置使用 **EmailJS** 服务，配置参数如下：

```r
CONFIG <- list(
  # 邮件功能开关
  EMAIL_ENABLED       = TRUE,                    # 设为 FALSE 可关闭邮件功能

  # EmailJS 配置（已从 history/index.html 复用）
  EMAILJS_SERVICE_ID  = "service_j2ak28v",      # 服务ID
  EMAILJS_TEMPLATE_ID = "template_ol3ws9o",     # 模板ID
  EMAILJS_PUBLIC_KEY  = "bGbqCw1wlTrkCwfFo"     # 公钥
)
```

### 接收邮箱配置

**重要提示**：邮件接收地址在 EmailJS 模板中配置，不在本脚本中。

要修改接收邮箱，请：

1. 登录 [EmailJS 控制台](https://dashboard.emailjs.com/)
2. 找到模板 `template_ol3ws9o`
3. 修改模板中的收件人邮箱地址
4. 保存模板

根据您提供的 HTML 文件，当前配置的邮箱应该已经在 EmailJS 后台设置好。

## 使用方法

### 启用邮件（默认已启用）

正常运行脚本即可，搜索完成后会自动发送邮件：

```bash
Rscript download_papersv13.R "machine learning" 10 30
```

### 临时禁用邮件

如果某次搜索不想发送邮件，可以修改脚本中的配置：

```r
EMAIL_ENABLED = FALSE
```

或者在脚本中注释掉邮件发送部分（第1457-1460行）。

## 运行示例

```bash
$ Rscript download_papersv13.R "starch" 5 10

[23:45:12] ℹ 关键词: "starch" | 每源: 5 | 最近 10 天
[23:45:15] ✔ Semantic Scholar: 找到 5 篇
[23:45:20] ✔ Europe PMC: 找到 4 篇
...
[23:46:30] ℹ CSV / JSON 已保存
[23:46:30] ℹ 报告已保存

[23:46:32] ℹ 准备发送邮件报告...
[23:46:32] ℹ 正在生成邮件报告...
[23:46:35] ✔ 邮件发送成功！

✅ 完成!
  论文: 15篇 | 有摘要: 13篇 | PDF: 8个
  输出: output/starch_papers
```

## 邮件发送失败排查

如果邮件发送失败，请检查：

1. **网络连接**
   - 确保能够访问 `https://api.emailjs.com`
   - 检查防火墙设置

2. **EmailJS 配置**
   - 确认 Service ID、Template ID、Public Key 正确
   - 登录 EmailJS 检查服务状态
   - 检查 EmailJS 账号是否有剩余配额（免费版每月200封）

3. **模板配置**
   - 确认模板中有 `{{message}}` 变量
   - 确认收件人邮箱已正确配置

4. **curl 工具**
   - 确保系统已安装 `curl` 命令
   - macOS/Linux 默认自带

## EmailJS 模板建议

为了正确接收邮件，您的 EmailJS 模板应包含以下内容：

```html
<h2>文献搜索报告</h2>

<pre>
{{message}}
</pre>
```

其中 `{{message}}` 变量会被替换为完整的文献列表。

## 注意事项

1. **邮件长度限制**
   - 为避免邮件过长，最多发送20篇文献详情
   - 如果搜索结果超过20篇，会在邮件末尾注明省略的数量

2. **摘要长度**
   - 每篇文献的摘要限制在300字符以内
   - 完整摘要可查看生成的 CSV/JSON 文件

3. **发送频率**
   - EmailJS 免费账号限制：200封/月
   - 建议合理使用，避免频繁搜索

4. **隐私安全**
   - API Key 等敏感信息已配置在脚本中
   - 如需更换服务，请及时更新配置

## 关闭邮件功能

如果您不需要邮件功能，可以：

**方法1：配置文件修改**
```r
EMAIL_ENABLED = FALSE
```

**方法2：注释代码**
在 `main()` 函数中注释掉以下代码（第1457-1460行）：
```r
# if (CONFIG$EMAIL_ENABLED) {
#   log_msg("准备发送邮件报告...")
#   send_email_report(kw, all, sum(all$pdf_ok), sum(all$pdf_mb, na.rm = TRUE))
# }
```

## 技术细节

邮件功能使用 EmailJS REST API 实现，通过 `curl` 命令发送 POST 请求：

```bash
curl -X POST 'https://api.emailjs.com/api/v1.0/email/send' \
  -H 'Content-Type: application/json' \
  -d '{
    "service_id": "service_j2ak28v",
    "template_id": "template_ol3ws9o",
    "user_id": "bGbqCw1wlTrkCwfFo",
    "template_params": {
      "to_name": "用户",
      "message": "文献详情..."
    }
  }'
```

## 常见问题

### Q: 为什么没有收到邮件？

A: 请检查：
- EmailJS 后台的收件人邮箱配置是否正确
- 垃圾邮件文件夹
- EmailJS 服务状态和配额

### Q: 可以修改邮件格式吗？

A: 可以。修改 `send_email_report()` 函数中的 `email_body` 构建逻辑。

### Q: 可以同时发送到多个邮箱吗？

A: 可以。在 EmailJS 模板中添加多个收件人，或创建多个模板分别发送。

### Q: 邮件发送会影响脚本运行时间吗？

A: 影响很小（约1-3秒）。邮件发送在所有搜索和下载完成后进行，不影响主要功能。

---

**最后更新**: 2026-02-18
**版本**: v13.1（新增邮件功能）
